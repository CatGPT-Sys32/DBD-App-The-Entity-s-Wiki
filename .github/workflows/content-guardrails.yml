name: Content Guardrails

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  guardrails-disabled:
    name: Guardrails Disabled
    runs-on: ubuntu-latest
    steps:
      - name: CI notice
        run: echo "Content Guardrails checks are currently disabled."
